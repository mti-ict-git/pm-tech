import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.merdekatsingshan.pmtech',
  appName: 'MTI Mobile PM',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;

